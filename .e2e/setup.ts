/**
 * E2E Test Global Setup
 *
 * Executes before all Playwright tests run.
 * Handles database initialization and schema setup.
 *
 * @module e2e/setup
 */

import { globalSetup } from "../shared/config/test";

export default globalSetup;